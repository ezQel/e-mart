import { CrInputDirective } from './cr-input.directive';

describe('CrInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CrInputDirective();
    expect(directive).toBeTruthy();
  });
});
